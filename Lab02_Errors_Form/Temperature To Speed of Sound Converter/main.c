//Hamza Malik. All rights reserved!#include <stdio.h>#include <math.h>void spsound(float temp_in_celsius);void main(){// This line of code asks the user for an input for the temperature in Celsiusprintf("Please enter a value for the temperature in Celsius:\n");float temp_in_celsius;scanf("%f", &temp_in_celsius);spsound(temp_in_celsius);}void spsound(float temp_in_celsius){float speed;/*This line of code converts input temperature (in Celsius) to Fahrenheit.Then it calculates speed which is converted from (ft/sec) into speed (km/h)*/speed = (1086 * (sqrt((5* ((temp_in_celsius * (9.0/5.0)) + 32) + 297) / (247.0))));//This line of code prints out the results from the calculations aboveprintf("Result: Speed of sound is %f km/h ", speed);}