//Hamza Malik. All rights reserved!#include <stdio.h>intmain (void){/*Constants enclosed in braces which will besorted in an ascending order*/int a[5] = {24, 11, 57, 17, 46};int temp = 0;/*Sorts the array and puts it into an ascendingorder meaning that the output will be fromlowest to highest so the output will be 11,17, 24, 46, & 57 when we run the program*/for (int i = 0; i<5; i++){for (int j = i+1 ; j<5; j++){if(a[i] > a[j]){temp = a[i];a[i] = a[j];a[j] = temp;}}}/* Displays "Elements in ascending order"as well as the output when we run theprogram*/printf("Elements in ascending order: \n");for (int i = 0; i <5; i++){printf("%d ", a[i]);}return (0);}